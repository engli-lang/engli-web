{
  "name": "engli-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "echo \"real lmao\" > dist/.nojekyll && gh-pages -d dist"
  },
  "devDependencies": {
    "typescript": "^5.0.2",
    "vite": "^4.4.5"
  },
  "dependencies": {
    "@types/node": "^20.5.8",
    "gh-pages": "^6.0.0"
  }
}